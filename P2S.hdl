
CHIP P2S {
    IN in[4], load;
    OUT out[4];

    PARTS:
    BitRegister(in=in[0], load=load, out=out[0]);
    BitRegister(in=in[1], load=load, out=out[1]);
    BitRegister(in=in[2], load=load, out=out[2]);
    BitRegister(in=in[3], load=load, out=out[3]);
}
